name: basem_lg
description: BASEM LG - Arabic local network device discovery app.
publish_to: "none"
version: 12.0.1+13

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_device_discovery: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
