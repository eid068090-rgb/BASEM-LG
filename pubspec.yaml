name: ALSAMAN

description: ALSAMAN - Advanced Local Network Device Discovery

publish_to: "none"

version: 12.0.1+12001

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # اكتشاف أجهزة وخدمات الشبكة المحلية
  flutter_local_device_discovery: ^2.0.0

  # فتح الروابط والمكالمات وواتساب وفيسبوك
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
