name: basem_device_finder
description: Local-network device discovery using Ubiquiti UDP/10001 and mDNS, with KT-708 support.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  multicast_dns: ^0.3.3

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
